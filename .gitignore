# ---------------------------------------------------------------------------
# BigPixels 放大台
#
# 仓库里只放代码和清单，不放任何「下下来就有、或者跑一跑就有」的东西。
# 原本目标是克隆下来不到 2 MB，其余第一次启动自己会补上。
# 2026-09-22 起多了两份**展示用固定素材**，不进本文件末尾那套「不进仓库」的规则：
#   docs/promo/    README 配图 5 张，约 5 MB
#   插画.png        上文那张插画 4× 超分后的成图，3372 × 5056，约 20 MB
# 两者都是给人看的成品，不随代码变动，也不属于「下下来就有、跑一跑就有」的东西。
# 除这两处之外，仓库仍按小仓库办。
# ---------------------------------------------------------------------------

# 权重（约 292 MB）。不进仓库的原因有两个：GitHub 单文件超 100 MB 就直接拦，
# 而且每次改代码都重传一遍权重也不合理。
# 启动脚本会照 download_models.py 里的清单自检，缺什么下什么。
/models/

# 顺手兜一层：权重文件不管被放到哪儿都不该进仓库
*.onnx

# 运行环境与依赖（几百 MB，且各机器各装一份）
/.venv/
/venv/

# 产物与中间缓存
/outputs/
/.cache/
/dist/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# 编辑器 / 操作系统
.vscode/
.idea/
.DS_Store
Thumbs.db
desktop.ini

# 工作区里的 agent 记忆与本地笔记，不属于代码
/.workbuddy/
/Anthropic/

# 下载中途的半截文件（正常会被改名或清掉，留个保险）
*.part

# 用户自己的对比素材（一次性，不进仓库）
/对比/
/细节优化/
/原始数据/
/输入.jpg

# 宣传图的**母版**也不进仓库：一张没放大过的源图，属于用户自己的素材。
# 仓库里放的是由它导出的成品图（docs/promo/），源图不进。
/宣传/
/宣传.jpg
